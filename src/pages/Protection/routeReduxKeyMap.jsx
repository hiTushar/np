const routeReduxKeyMap = {
    app: application_control,
    fort: zv_fort_protection,
    cloud: cloud_protection,
    os: os_protection,
    adblock: ad_blocker,
    online: online_protection,
    email: email_protection,
    lan: lan_protection,
    ids: ids,
    backup: data_backup
}

export default routeReduxKeyMap;
// now redundant as route key and redux key are same