export default function TableFlex(props) {
    // TODO: use this table component made of all <div>s for the outbound applications in the firewall features section 
    // or try react-table as well
    return (
        <div className="npav-tableFlex">

        </div>
    )
}
